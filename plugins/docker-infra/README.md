# docker-infra

Plugin de infraestructura: adapter compartido al docker remoto (host x86
Tailscale) via SSH. Consumido por hosting, proxy y mail. Read-only por
defecto; operaciones mutantes solo via acciones explicitas.
